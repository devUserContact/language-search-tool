import type { NextPage } from 'next'
import styles from '../../styles/Home.module.css'
import SearchForm from '../SearchForm'

const Iframes: NextPage = () => {

	return (
		<>
			<div className={styles.container}>
			</div>
		</>
	) 
}
export default Iframes
